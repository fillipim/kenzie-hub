import styled from "styled-components"
import { StyledTitle } from "../../styles/typography"

export const ErrorMessage = styled(StyledTitle)`
        text-align: start;
        display: block;
        cursor: pointer;
        min-height: 14px;
        transition: all .3s;
`