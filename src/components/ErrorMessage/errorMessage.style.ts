import styled from "styled-components"

export const ErrorMessage = styled.span`
        text-align: start;
        display: block;
        cursor: pointer;
        min-height: 14px;
        color: #E83F5B;
        font-size: 12px;
        font-weight: 400;
`